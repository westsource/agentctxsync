# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Local scratch (test cookies etc.)
jar*.txt

# Environment & secrets (NEVER commit these)
.env
.env.*
!.env.example
**/.env.example
server/.env
server/.env.*
!server/.env.example

# Local agent data
.env.reasonix/
.reasonix/
.workbuddy/
.workbuddy-ai/

# Local Reasonix sandbox config (may contain local-only paths)
reasonix.toml

# Local workspace guidance (AGENTS.md) — held locally, never committed:
# it contains machine-local deployment rules for the 236 server.
AGENTS.md

# OS
Thumbs.db
.DS_Store
